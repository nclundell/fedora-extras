name: yazi copr rebuild
on:
  push:
    branches:
      - main
    paths:
      - yazi/yazi.spec
  workflow_dispatch:

jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: curl
        uses: enflo/curl-action@master
        with:
          curl: -X POST https://copr.fedorainfracloud.org/webhooks/github/192871/7bf3c613-5906-4b97-afe3-d79aa232a7e7/yazi/
